# Colab Notebooks

- `chronos_k1_quickstart.ipynb`: **Exp7-only** runner for
  `benchmarks/experiment_7_metric_controlled_normalization.py`.
  Includes smoke and full-run cells.
- `chronos_embodied_toy.py`: portable S0-E0 mirror for Colab. The canonical
  implementation lives in `chronos/embodied_toy/` and `chronos/s0/`.
- `chronos_embodied_sim.py`: portable S0-E1 mirror for Colab. The canonical
  implementation lives in `chronos/embodied_toy/` and `chronos/s0/`.
- `chronos_embodied_active.py`: portable S0-E2 mirror for Colab. The canonical
  implementation lives in `chronos/embodied_toy/` and `chronos/s0/`.
- `chronos_embodied_active2.py`: portable S0-E2b mirror for Colab. The
  canonical implementation lives in `chronos/embodied_toy/` and `chronos/s0/`.
- `chronos_k3_active.py`: portable K3-E2b active topology regime-search mirror
  for Colab. The canonical implementation lives in `chronos/k3/`.
