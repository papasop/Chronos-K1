name: claims-tests

on:
  push:
  pull_request:

jobs:
  claims-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m unittest chronos.claims.tests.test_claims
